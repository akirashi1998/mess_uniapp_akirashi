<view class="container">

  <view class="banner">
  {{loginname}}</view>

  <view class="employdetails">{{loginname}}</view>

  <view class="workitem" wx:for="{{result.data}}">
    <view class="name">{{result.data[index].workname}}</view>
    <view class="time">{{result.data[index].oeder}}</view>
  </view>
</view>